/*global define*/
define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var AssetsModel = Backbone.Model.extend({
        defaults: {
        },

    });

    return AssetsModel;
});
